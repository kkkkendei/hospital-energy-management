# 服务器配置
server.port=8081

# 数据库配置 (MySQL)
spring.datasource.url=jdbc:mysql://47.122.26.159:3306/hospital_energy?useUnicode=true&characterEncoding=utf8&autoReconnect=true&zeroDateTimeBehavior=convertToNull&serverTimezone=UTC&useSSL=true
spring.datasource.username=root
spring.datasource.password=5185021
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# MyBatis 配置 (如果使用 MyBatis)
mybatis.mapper-locations=classpath:mapper/*.xml
mybatis.configuration.map-underscore-to-camel-case=true

# JPA 配置 (如果使用 JPA)
# spring.jpa.hibernate.ddl-auto=update # 开发时可以是 update 或 create, 生产环境建议 validate 或 none
# spring.jpa.show-sql=true
# spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQL8Dialect

# Spring Security (可选, JWT 相关配置)
# jwt.secret=your-very-long-and-secure-secret-key-for-jwt # JWT 密钥，请修改为一个复杂且安全的字符串
# jwt.expirationMs=86400000 # JWT 过期时间 (例如: 24小时)

# 应用名
spring.application.name=hospital-energy 